"""jobtailor package.

An `__init__.py` marks a directory as an importable Python package (roughly the
namespace/assembly boundary in .NET terms). It can be empty; here it just holds
the version string.
"""

__version__ = "0.1.0"
